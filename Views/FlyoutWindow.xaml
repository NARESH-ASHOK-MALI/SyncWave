<ui:FluentWindow x:Class="SyncWave.Views.FlyoutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:SyncWave.ViewModels"
        Title="SyncWave Flyout" 
        Height="Auto" Width="300" MinHeight="0"
        SizeToContent="Height"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        Background="Transparent"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        FontFamily="Segoe UI Variable, Segoe UI"
        BorderThickness="1"
        BorderBrush="{DynamicResource SurfaceStrokeColorDefaultBrush}">
    <ui:FluentWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </ui:FluentWindow.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <TextBlock Text="SyncWave" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,12" />

        <ItemsControl Grid.Row="1" ItemsSource="{Binding Devices}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,0,0,12" Visibility="{Binding IsSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Right" Text="{Binding Volume, StringFormat='{}{0:F0}'}" FontSize="12" Margin="8,0,0,4" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextAlignment="Right" />
                            <TextBlock Text="{Binding FriendlyName}" FontSize="12" Margin="0,0,0,4" TextTrimming="CharacterEllipsis" />
                        </DockPanel>
                        <Slider Grid.Row="1" Value="{Binding Volume}" Minimum="0" Maximum="{Binding DataContext.MaxVolumeLimit, RelativeSource={RelativeSource AncestorType=Window}}" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <TextBlock Grid.Row="1" Text="No devices synced" FontStyle="Italic" Opacity="0.6" HorizontalAlignment="Center" Margin="0,20,0,20" x:Name="NoDevicesText" Visibility="Collapsed" />
    </Grid>
</ui:FluentWindow>
